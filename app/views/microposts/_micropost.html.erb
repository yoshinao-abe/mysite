
 <li id="micropost-<%= micropost.id %>" class="list-unstyled border-top border-secondary">
 <span　class="row mt-2">
  <%= image_tag micropost.user.image.url, class: 'profile-post-image mt-2 mr-2' %>
  <%= link_to micropost.user.name, micropost.user %>
 </span>
  <%= link_to "詳細", "/microposts/#{micropost.id}", method: :get, class: "btn btn-primary float-right mt-2" %>
  <div class="col mb-2 mt-2">
   <span class="content m-2">
    <%= micropost.content %>
    <%= image_tag micropost.picture.url if micropost.picture? %>
   </span>
  </div>
  <div class="row">
   <span class="timestamp mb-1 col-sm">
      Posted <%= time_ago_in_words(micropost.created_at) %> ago.
      <% if current_user?(micropost.user) %>
        <%= link_to "delete", micropost, method: :delete,
                                         data: { confirm: "削除してよろしいですか？" } %>
      <% end %>
   </span>
   <div class="text-right">
    <p class="btn btn-info mb-2"><%= raw(micropost.tag_list.map { |t| link_to t, tag_path(t), class:"text-reset text-white" }.join(', ')) %></p>
   </div>
  </div>
</li>