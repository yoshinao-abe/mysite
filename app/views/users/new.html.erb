<% provide(:title, "Signup") %>
<% provide(:url, signup_path) %>
<% provide(:button_text, "アカウントを作る！") %>

<div class="row justify-content-center">
  <div class="col-md-8 bg-light">
    <h1 class="text-center font-weight-bolder font-italic bg-success col-auto">GAMERES</h1>
  
    <h1>Sign Up</h1>

      <div class="row">
        <div class="col">
          <%= render 'form' %>
        </div>
      </div>
  </div>
</div>